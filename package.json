{
  "name": "pi-foundry",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "start:backend": "node src/backend.mjs",
    "test": "node --test --test-reporter=spec test/*.test.mjs",
    "emit:contract": "node scripts/emit-contract.mjs",
    "runtime:build": "scripts/runtime-image-build.sh",
    "runtime:smoke": "scripts/runtime-image-smoke.sh"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "dependencies": {
    "@azure/identity": "^4.13.1",
    "@azure/storage-blob": "^12.31.0"
  }
}
